# ──────────────────────────────────────────────
# src/models/ — Model Layer
# ──────────────────────────────────────────────
# Everything related to building, training, evaluating, and
# saving machine learning models lives here.
#
# Files in this package:
#   trainer.py     → Model training and hyperparameter tuning
#   evaluator.py   → Metrics computation, classification reports, plots
#   predictor.py   → Load a saved model and make predictions (inference)
