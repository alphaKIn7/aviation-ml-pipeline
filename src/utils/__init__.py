# ──────────────────────────────────────────────
# src/utils/ — Shared Utilities
# ──────────────────────────────────────────────
# Helper functions used across ALL layers.
# Keep this lean — only truly shared code belongs here.
#
# Files:
#   logger.py       → Centralized logging setup
#   io_handler.py   → File I/O helpers (save/load YAML, pickle, etc.)
#   metrics.py      → Custom metric functions
#   config.py       → Config loading and validation
