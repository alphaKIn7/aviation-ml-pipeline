# ──────────────────────────────────────────────
# src/ — The core importable Python package
# ──────────────────────────────────────────────
# Making this directory a Python package allows:
#   from src.data.ingestion import load_data
#   from src.models.trainer import train_model
#
# This is the "library" of your project. Scripts and notebooks
# CALL into this package — they never contain business logic themselves.
