# ──────────────────────────────────────────────
# src/data/ — Data Layer
# ──────────────────────────────────────────────
# Everything related to getting data IN, validating it,
# and transforming it into a clean format lives here.
#
# Files in this package:
#   ingestion.py      → Load data from CSV/DB/API
#   validation.py     → Schema checks, null checks, data quality gates
#   preprocessing.py  → Cleaning, encoding, scaling, imputation
